// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/rust
{
	"name": "Rust",
	"build": {
		"dockerfile": "Dockerfile"
	},

	// Mount a directory from the host to the container.
	//"mounts": [
	//	"source=/Users/atin.malaviya/src/pingap,target=/workspace/pingap,type=bind"
	//],

    // Add these run arguments for privileged mode and port mapping
    "runArgs": ["--privileged", "--cap-add=NET_ADMIN"],

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [443, 4000],

	// Use 'postCreateCommand' to run commands after the container is created.
	// "postCreateCommand": "rustc --version",

	// Configure tool-specific properties.
	"customizations": {
    "vscode": {
        "extensions": [
            "codeium.codeium-extension",
			"rust-lang.rust-analyzer",
			"ms-azuretools.vscode-docker",
			"ms-vscode.makefile-tools",
			"vadimcn.vscode-lldb"
        ]
    }
}

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
